# V2 format config with unknown adapter - should fail validation
conversation:
  timeout: 30s
  mode: parallel
agents:
  - id: agent-1
    type: nonexistent
    adapter: nonexistent
    name: Unknown Agent
    model: unknown-model
    config:
      system_prompt: "test"
